---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: debian-9
      image: debian
      image_version: 9
ansible:
  group_vars:
    all:
      resolv_file: /tmp/resolv.conf
verifier:
  name: testinfra
