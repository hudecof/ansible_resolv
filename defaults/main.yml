---
# defaults file for resolv

resolv_packages: "{{ __resolv_packages }}"

resolv_file: /etc/resolv.conf

resolv_domain: null

resolv_search: []

resolv_nameserver:
  - 178.22.122.100
  - 185.51.200.2

resolv_sortlist: []
resolv_options:
  - rotate
